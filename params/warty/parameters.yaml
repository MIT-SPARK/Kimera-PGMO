frame_id: "world"
d_graph_resolution: 5.0 # meters
compression_time_horizon: 20 # seconds 
embed_trajectory_delta_t: 5 # seconds (prints warning if delta exceeds, not strictly enforced. ) 
rpgo:
  translation_threshold: 0.01
  rotation_threshold: 0.005
